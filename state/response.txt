## Subtask Complete: Return counts: total, by_category, by_automation

### Status: ALREADY IMPLEMENTED

The `/api/replies/stats` endpoint already returns all three required counts:

1. **total** - Line 550 in replies.py calculates total count of ProcessedReply records
2. **by_category** - Lines 553-564 groups replies by category and returns dict like `{"interested": 5, "not_interested": 2}`
3. **by_automation** - Lines 634-652 groups by automation_id and includes name, returning dict like `{"1": {"id": 1, "name": "Test", "count": 10}}`

### Schema (reply.py lines 185-196):
```python
class ProcessedReplyStats(BaseModel):
    total: int = 0
    by_category: dict = Field(default_factory=dict)
    by_automation: dict = Field(default_factory=dict)
    by_status: dict = Field(default_factory=dict)
    today: int = 0
    this_week: int = 0
    sent_to_slack: int = 0
    pending: int = 0
    approved: int = 0
    dismissed: int = 0
```

### Test Coverage (test_replies.py lines 321-368):
- `test_stats_empty` - verifies empty state returns proper defaults
- `test_stats_with_data` - verifies by_category and by_automation counts are correct

### Endpoint Usage:
```
GET /api/replies/stats
GET /api/replies/stats?automation_id=1
GET /api/replies/stats?campaign_id=abc123
```

No code changes required - functionality was already complete.
