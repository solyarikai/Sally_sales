SUBTASK COMPLETED: Read backend/app/services/reply_processor.py and analyze error handling

## Error Handling Analysis for reply_processor.py

### Overview
The file contains 3 main async functions with error handling patterns.

### Error Handling Summary:

#### 1. classify_reply() (lines 84-142)
- **Pre-check**: Checks `openai_service.is_connected()` before calling API (line 98)
- **Graceful fallback**: Returns default "other" category with low confidence if not connected
- **Try/except**: Catches all exceptions (line 136)
- **Recovery**: Returns safe default values on error with error message in reasoning field
- **Status**: GOOD - graceful degradation, no crashes

#### 2. generate_draft_reply() (lines 179-255)
- **Early return**: Skips draft for out_of_office category (line 202)
- **Pre-check**: Checks OpenAI connection (line 209)
- **Graceful fallback**: Returns placeholder text if not connected
- **Try/except**: Catches all exceptions (line 249)
- **Recovery**: Returns error placeholder in body field with "error" tone
- **Status**: GOOD - graceful degradation

#### 3. process_reply_webhook() (lines 258-488)
- **Comprehensive logging**: Logs payload keys, event type, full payload (lines 272-276)
- **Null safety**: Uses `or` chaining for field extraction (e.g., lines 283-287)
- **HTML sanitization**: Strips HTML tags from body (lines 304-310)
- **Early return**: Returns None if no lead_email (line 345-346)
- **Try/except**: Main try block wraps entire processing (line 278)
- **Error tracking**: On failure, updates automation.total_errors and last_error (lines 479-486)
- **Nested try/except**: Google Sheets logging has its own error handling (line 463-464)
- **Session management**: Calls session.rollback() on failure (line 487)
- **Status**: GOOD - comprehensive with error tracking

### Potential Issues Found:
1. **Silent failures in nested operations**: Lines 485-486 use bare `except: pass` which swallows errors silently
2. **No retry logic**: API calls to OpenAI fail once and give up
3. **JSON parsing**: Lines 122 and 241 parse JSON without specific exception handling for JSONDecodeError

### Recommendations:
1. Add specific `json.JSONDecodeError` handling for more informative error messages
2. Consider adding retry logic for transient OpenAI failures
3. Replace bare `except: pass` with proper logging

### Conclusion:
Error handling is generally robust with graceful degradation patterns. The code won't crash on errors and provides useful fallback values. The main webhook processor properly tracks errors on the automation record for monitoring.
