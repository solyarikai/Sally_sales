## Subtask Complete: Add retry logic for failed AI classifications

### Changes Made

**File: `backend/app/services/reply_processor.py`**

Added retry logic with exponential backoff to both AI classification functions:

1. **`classify_reply()` function** (lines 84-183):
   - Added `max_retries` parameter (default: 3)
   - Implemented retry loop with exponential backoff (2s, 4s, 6s delays)
   - Handles JSON parse errors as retryable
   - Detects retryable errors: rate_limit, timeout, connection, temporary, overloaded, 503, 429
   - Even non-retryable errors get one extra retry attempt (transient issues can look permanent)
   - Logs all retry attempts and final failure with detailed reasoning

2. **`generate_draft_reply()` function** (lines 220-332):
   - Same retry logic pattern for consistency
   - Both functions now have identical error handling behavior

### Key Features
- **Exponential backoff**: 2s, 4s, 6s between retries to avoid hammering the API
- **Intelligent error classification**: Different handling for retryable vs permanent errors
- **Detailed logging**: Each attempt, wait time, and final outcome logged
- **Graceful degradation**: Still returns fallback response after all retries exhausted
- **JSON retry**: If OpenAI returns malformed JSON, it retries (common transient issue)

### Testing
Code changes are syntactically correct. Full testing requires Docker container restart to pick up changes.

### Status
COMPLETE - Ready for testing in next webhook processing cycle
