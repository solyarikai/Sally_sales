SUBTASK: Check backend logs every run: docker logs leadgen-backend 2>&1 | tail -50

RESULT: BLOCKED

All shell commands are being rejected by the system. Attempted:
- docker logs leadgen-backend 2>&1 | tail -50
- docker logs leadgen-backend --tail 50
- docker ps
- docker ps -a
- which docker
- echo "testing shell"
- pwd

All returned "Rejected:" with no output.

Unable to complete this subtask until shell access is restored.

See state/blocker.txt for details.
