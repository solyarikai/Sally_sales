---
name: whisper-transcribe
description: >-
  Транскрибирует аудио или видео файл в текст с помощью локального Whisper.
  Используй когда пользователь говорит "транскрибируй", "расшифруй",
  "переведи аудио в текст", "сделай текст из записи", "что там на записи",
  или даёт путь к .mp3/.mp4/.wav/.m4a/.ogg/.flac/.webm файлу с просьбой обработать.
  Также: "@file.mov транскрибируй", "transcribe this", "сделай транскрипцию",
  "расшифруй звонок", "что говорят на видео".
---

# Whisper Transcribe

Локальная транскрипция аудио/видео через OpenAI Whisper.

**Whisper**: `/Users/user/Library/Python/3.14/bin/whisper`

---

## Шаг 1 — Определи параметры

Из контекста разговора определи (не спрашивай, если очевидно):

| Параметр | По умолчанию |
|----------|-------------|
| Путь к файлу | Из сообщения пользователя |
| Язык | Авто-определение (не указывай `--language`) |
| Модель | `turbo` |
| Формат | `txt` |
| Куда сохранить | Рядом с исходным файлом |

Если пользователь явно указал язык — добавь `--language ru` или `--language en`.

---

## Шаг 2 — Запусти транскрипцию

```bash
/Users/user/Library/Python/3.14/bin/whisper "<путь>" --model turbo --output_format txt --output_dir "<папка>"
```

Варианты:

**С указанием языка:**
```bash
/Users/user/Library/Python/3.14/bin/whisper "<путь>" --model turbo --language ru --output_format txt --output_dir "<папка>"
```

**Перевод на английский (любой язык -> English):**
```bash
/Users/user/Library/Python/3.14/bin/whisper "<путь>" --model turbo --task translate --output_format txt --output_dir "<папка>"
```

**Субтитры:**
```bash
/Users/user/Library/Python/3.14/bin/whisper "<путь>" --model turbo --output_format srt --output_dir "<папка>"
```

---

## Шаг 3 — Покажи результат

1. Прочитай полученный файл
2. Покажи транскрипт пользователю
3. Если уместно — предложи сохранить в `sofia/transcripts/`

---

## Выбор модели

| Модель | Скорость | Качество | Когда |
|--------|----------|----------|-------|
| `tiny` | Мгновенно | Низкое | Черновик |
| `small` | Быстро | Хорошее | Короткие записи |
| **`turbo`** | **Быстро** | **Высокое** | **По умолчанию** |
| `large` | Медленно | Лучшее | Максимальное качество |

Если файл > 30 мин — предупреди, что займёт несколько минут.

## Форматы

`.mp3` `.mp4` `.wav` `.m4a` `.ogg` `.flac` `.webm` `.mkv` `.avi`
