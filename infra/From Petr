While automatically creating domains in Namecheap, set or set up them automatically, provide endpoints to answer such questions as endpoints for the following purposes like check all records, health check, just health check. What should health check should do? It should check all like SPF, DMARC, DKIM, DKIM, SPF, DMARC, DMARC records and also check domain redirect to like, to the given website. So it should be stored in database as well. I mean, these redirect rules, yeah. Separate table, note yourself as architecture guide. Provide this health endpoint and provide another endpoint to fix. So if health find any issues, fix these issues, you see? And so like agent using these endpoints can like use it, or even not agent, but cron job can use health and fix endpoints.

Also, inside yourself, like how to, what should be passed to these endpoints, like definitely the project should be passed, or decide as god yourself.