All blocked by auth. The entire loop is gated on S1 login.

## Results

| Step | Tool | Result | Detail |
|------|------|--------|--------|
| S1 | `login` | **FAIL** | `"Token must start with mcp_"` — email `pn@getsally.io` rejected, tool requires `mcp_` prefixed token |
| S2 | `configure_integration` (smartlead) | **FAIL** | `"Authentication required"` — no session from S1 |
| S3 | `configure_integration` (apollo) | **FAIL** | `"Authentication required"` — no session from S1 |
| S4 | `configure_integration` (openai) | **FAIL** | `"Authentication required"` — no session from S1 |
| S5 | `create_project` | **SKIP** | Blocked by S1 |
| S6 | `tam_gather` | **SKIP** | Blocked by S1 |
| S7 | `blacklist→approve→pre_filter→scrape` | **SKIP** | Blocked by S1 |
| S8 | `tam_analyze` | **SKIP** | Blocked by S1 |
| S9 | `get_context` | **SKIP** | Blocked by S1 |

**Root cause**: The `login` tool enforces `mcp_` prefix tokens only. The test script passes an email (`pn@getsally.io`), which the tool rejects. Either:
1. The login tool needs to accept email-based auth (for backward compat with test loops), or
2. The test script needs to pass a valid `mcp_` token instead of an email.
