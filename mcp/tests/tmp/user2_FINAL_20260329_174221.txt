I see you've provided a comprehensive MCP test plan with detailed test cases. To execute these tests, I need an MCP API token (starting with `mcp_`) to authenticate with the system.

**Do you have an MCP API token?** If not:

1. Go to http://46.62.210.24:3000/setup to generate one
2. Paste the token here (it will start with `mcp_`)

Once I have the token, I can:
- Login as services@getsally.io
- Configure the API integrations (SmartLead, Apollo, OpenAI, GetSales)
- Execute the full test sequence (T02 → T13 → SEQ → FINAL)
- Report results in the table format you specified

Which would you prefer:
1. **Provide the MCP token** — I'll run the entire test plan immediately
2. **Get help generating a token** — I can guide you through the setup at http://46.62.210.24:3000/setup
3. **Something else** — let me know what's needed
