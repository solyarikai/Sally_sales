Consider also cases directory, so that the case is like for the default flow initially, I assume that user simply writes you in text several words about what he needs to gather, but sometimes it's a case for a quick launch something, but usually user really preparing for the launch some kind of strategy, aligning with the client. So like preparing a big context, big examples of some companies so that you can use all this context to just directly gather, like enrich five top examples in Apollo to optimize search strategy from what user provides. So you should cover this case as well. So extend your filter, your agent system plan of how agent chain should behave and use to cover this case as well. When the user just tell you, hey, use my file name or file path to the file. to gather more contacts. And you can extract everything from this file. So you use like probably build everything for users that use in cloud code. So that we don't rely much on, on like how you separate, like whether user's agent work finishes and MCB work starts. As such files could be like huge files. And I'm not sure you can rely on GPT for all mini prompts here. So what would be your strategy? Write a great plan for this, considering building, providing clear border when agent, you know, I'm concerned about agent or not agent, since I need agent to, I need it to be processed perfectly. I need to, you know, perfectly provide all the context to MCB. So is it a task for like, should I build kind of processor or new agents in MCB to cover this use case? But my practical question is, yes, should I use this more agents with more smart models like GPT-5 or I can rely on a user's agent, considering and assuming that my user using cloud code with Opus and that like Opus will do it, will... Like summarize all these files and provide the context to MCP so that my small MCP small stupid agents with GPT-4 Mini will work with it. That's my central question, actually.